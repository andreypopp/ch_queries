(ocamllex lexer ulexer)

(menhir
 (modules parser uparser))

(library
 (name queries_syntax)
 (public_name queries.syntax)
 (flags
  (:standard -open ContainersLabels))
 (libraries containers pprint))
